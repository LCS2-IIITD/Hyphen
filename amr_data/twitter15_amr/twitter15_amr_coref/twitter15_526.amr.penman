# ::snt seems legit
# ::tokens ["seems", "legit"]
# ::lemmas ["seem", "legit"]
(c1-1 / seem-01
      :ARG1 (c1-0 / legitimate-02))

# ::snt ... looks totally fake
# ::tokens ["...", "looks", "totally", "fake"]
# ::lemmas ["...", "look", "totally", "fake"]
(c2-0 / look-02
      :ARG1 (c2-2 / fake-02
                  :degree (c2-1 / total)))

# ::snt codswallop!
# ::tokens ["codswallop", "!"]
# ::lemmas ["codswallop", "!"]
(c5-0 / codswallop
      :mode expressive)
